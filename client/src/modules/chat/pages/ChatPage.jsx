import { HomeNav, SideBar } from "../../ui/components";
import { Chat } from "../components/Chat";

export const ChatPage = () => {
  return (
    <div>
      <SideBar />
      <Chat />
    </div>
  );
};
