import { Verification } from "./associationModel";

// export const createAssociation = async (userId, companyId) => {
//     try {
//         const verification = await Verification.create({ userId, companyId })
//         return verification
//     } catch (error) {
//         throw new Error(error.message)
//     }
// }

